```
# PasswordManager 设计文档

## 产品定位

一个本地优先、离线可用的桌面密码管理器，核心体验是：安全、简单、快速查找。

## 技术方案

- 单个 `PasswordManager.exe` 分发
- 数据存储在 `%APPDATA%/PasswordManager/data.enc`
- 用户只看到一个 exe 文件

## 技术栈

- Python + PySide6（GUI）
- SQLite（本地数据库）
- cryptography（AES-GCM 加密）
- PyInstaller（打包单 exe）

## 功能范围（v1）

### 必须实现

- 主密码设置与解锁
- 添加/编辑/删除条目（网站/软件名、用户名、密码）
- 按网站/软件名搜索
- 复制密码到剪贴板

### 暂不做

- 自动清除剪贴板
- 生物识别
- 云端同步（架构预留）

## 数据模型

### 条目（entries）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | 唯一标识 |
| title | TEXT | 网站/软件名 |
| username | TEXT | 用户名 |
| password | TEXT | 密码（加密存储） |
| url | TEXT | 网址（可选） |
| notes | TEXT | 备注（可选） |
| created_at | TEXT | 创建时间 |
| updated_at | TEXT | 更新时间 |

### 配置（config）

| 字段 | 说明 |
|------|------|
| master_password_hash | 主密码哈希（不可逆） |
| salt | 盐值 |
| kdf_iterations | PBKDF2 迭代次数 |
| version | 数据版本号 |

## 安全设计

### 主密码流程

1. 首次运行：用户设置主密码
2. 使用 PBKDF2 派生加密密钥
3. 存储验证用哈希（不可逆）
4. 每次启动：输入主密码解锁

### 数据加密

- 使用 AES-GCM 加密敏感字段（username/password）
- 本地数据库只保存密文

## 界面设计

### 登录窗口

- 输入主密码
- 首次使用：设置主密码流程

### 主窗口

- 顶部：搜索框（按网站/软件名）
- 左/中：条目列表（title + username）
- 右：详情区（显示密码为 ****，可复制）
- 工具栏：新增、编辑、删除

## 项目结构
```

PasswordManager/
├─ app/
│  ├─ main.py                 # 入口
│  ├─ ui/
│  │  ├─ login_window.py      # 主密码登录
│  │  ├─ main_window.py       # 主界面
│  │  └─ dialogs.py           # 新增/编辑/删除弹窗
│  ├─ core/
│  │  ├─ crypto.py            # 加密/解密
│  │  ├─ db.py                # SQLite 数据层
│  │  └─ models.py            # 数据模型
│  └─ utils/
│     ├─ clipboard.py         # 复制到剪贴板
│     └─ paths.py             # AppData 路径管理
├─ assets/
│  └─ icon.ico                # exe 图标
├─ requirements.txt
└─ build.spec                 # PyInstaller 打包配置

```
## 打包方案

使用 PyInstaller：

- `--onefile` 单文件
- `--windowed` 隐藏控制台
- `--icon icon.ico` 自定义图标

## 交付验收标准

- 双击 exe 后进入主密码界面
- 首次使用可设置主密码
- 可正常新增/编辑/删除条目
- 搜索可用
- 复制密码可用
- 重启软件数据仍在
- 用户只看到一个 exe

## 云端同步（预留）

第一版不实现，但架构上预留"同步模块"：

- 本地保存主数据
- 同步模块负责打包加密数据、推送/拉取云盘
- 同步冲突策略：最新修改胜出
```